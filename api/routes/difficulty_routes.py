# Defining all the routes for the difficulty model
    # Routes
        # GET /difficulties
            # Returns all difficulties
        # POST /difficulties
            # Creates a new difficulty
        # GET /difficulties/:id
            # Returns a single difficulty
        # PUT /difficulties/:id
            # Updates a single difficulty
        # DELETE /difficulties/:id
            # Deletes a single difficulty