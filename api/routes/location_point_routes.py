# Defining all the location point routes
    # Routes
        # GET /location_points
            # Returns all location points
        # POST /location_points
            # Creates a new location point
        # GET /location_points/:id
            # Returns a single location point
        # PUT /location_points/:id
            # Updates a single location point
        # DELETE /location_points/:id
            # Deletes a single location point