# Defining all the routes for the location model
    # Routes
        # GET /locations
            # Returns all locations
        # POST /locations
            # Creates a new location
        # GET /locations/:id
            # Returns a single location
        # PUT /locations/:id
            # Updates a single location
        # DELETE /locations/:id
            # Deletes a single location
