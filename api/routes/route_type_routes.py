# Handles all the endpoints for the route type model
    # Routes
        # GET /route_types
            # Returns all route types
        # POST /route_types
            # Creates a new route type
        # GET /route_types/:id
            # Returns a single route type
        # PUT /route_types/:id
            # Updates a single route type
        # DELETE /route_types/:id
            # Deletes a single route type